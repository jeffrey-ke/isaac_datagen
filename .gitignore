# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
*.exr

# Heavy artifacts — synced via `art` (.artifacts.yaml); HF is the source of truth.
# Anchored + slash-free so the bulk-storage symlink form is matched too.
/assets
/datasets
/src/isaac_datagen/datasets
